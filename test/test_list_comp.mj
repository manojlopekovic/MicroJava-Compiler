program Examples
{

	
	void main()
	int arr1[], arr2[], arr3[];
	int ia, ib;
	char ca, cb;
	char carr1[], carr2[];
	{
		// Break outside for loop
		//break;
		
		// Continue outside for loop
		//continue;
		
		// List comperhension: different array types
		// arr1 = [for a in carr2];
		// Undefined variable a
		// arr1 = [for a in arr2];
		// List comperhension: expression type doesn't match array type
		// arr1 = [for ca in arr2];
		// List comperhension: Trying to use variable as array
		// arr1 = [for ca in ib];
		// ib = [for ca in arr1];
		// List comperhension: Multiple variables declared
		// arr1 = [for ia + ib in arr2s];
		
		
		arr2 = range(15);
		arr1 = new int[15];
		arr1 = [for ia + 1 in arr2];
		print(arr2);
		print(eol);
		print(arr1);
		print(eol);
		
		arr2 = range(30);
		arr3 = new int[30];
		// List comperhension: Multiple variables declared
		// arr3 = [for ia % 5 in arr2 if ia + ib - 5 < 17];
		arr3 = [for ia % 5 in arr2 if ia - 5 < 17];
		print(arr2);
		print(eol);
		print(arr3);
	}
}	